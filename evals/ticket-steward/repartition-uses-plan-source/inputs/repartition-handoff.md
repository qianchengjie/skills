你正在使用 ticket-steward 落实已确认的拆分。当前停在需要调用 to-tickets 生成新票的边界；你的下一条回复将原样成为 to-tickets 的调用输入。请给出这份实际交接输入；如果当前不能调用，说明具体阻塞。不要评审或改进 skill。

项目使用支持 Parent、native sub-issue 和 blocking links 的 tracker。to-tickets 可读取你传入的 issue source；创建后会返回实际 ID。下一批编号已预留为 27—30。

现有票据资料：
- #19「检出阅读与定位」包含：右栏检出阅读、逐次定位与高亮、定位提示与关闭记忆、消息旁标记与阅读位置四项行为。当前为 ready-for-agent，前置为 #23。
- #23「查询结果与当前会话」提供四项行为需要的结果和会话身份。
- #18「完整复盘验收」覆盖四项行为共同运行时右栏、消息、提示的对应，以及与其它页面区域的联动。

已确认的拆分方案来自 decomposition.md：
- #27「右栏检出阅读」：完整交付分组、顺序、重复命中、说明缺失与空结果的展示和验收；blocked by #23。
- #28「逐次定位与高亮」：完整交付首次点击、循环计数、目标定位、高亮、缺失目标与迟到动作隔离的行为和验收；blocked by #27。
- #29「定位提示与关闭记忆」：完整交付提示条件、关闭写入、重新进入和切换身份后沿用的行为和验收；blocked by #27。
- #30「消息旁标记与阅读位置」：完整交付消息旁标记、切换会话的阅读起点以及局部更新保持位置的行为和验收；blocked by #23。
- #18 的上述组合验收范围与其它既有前置继续沿用。
方案的范围、验收、编号与前置已获确认。生成服务现在可用，请继续。
