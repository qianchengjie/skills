# 规则使用记录

本上下文定义真实任务中规则使用记录的领域语言。它关注 consumer 留下了什么可信记录，不讨论规则是否有效。

## Language

**规则使用记录**：
consumer 针对某个可验证规则版本留下的一条长期事件记录。它证明 consumer 记录了该事件，不证明事件中的语义判断正确或规则产生了效果。
_Avoid_: 规则效果、有效性得分、反事实证据

**规则版本身份**：
由规则仓身份、规则仓提交、规则编号和规则内容摘要共同确定的精确规则版本。缺少任一部分时，不能形成可信的规则使用记录。
_Avoid_: 规则编号、临时规则版本、当前规则

**运行投影**：
一个 consumer 阶段达到稳定终态后形成的不可变规则使用记录集合。它是多条逻辑记录的容器，不改变各条记录独立校验和作废的边界。
_Avoid_: 运行时流水、可变 ledger、评测结果

**语义判断记录**：
consumer 对适用性、审查结果、修复或无法验证原因留下的声明。它可以长期保存，但仍然是待解释和复核的判断，而不是客观事实。
_Avoid_: 已证实事实、规则效果证据

**作废记录**：
因规则来源提交或规则版本身份无法再次验证而不再参与后续使用的历史记录。原始记录仍然保留，作废不等于删除或改写。
_Avoid_: 已删除记录、已修复记录
