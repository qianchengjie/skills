interface:
  display_name: "开发交付"
  short_description: "在隔离 workspace 中执行 caller 定义的开发范围并返回可审计交付结果"
  default_prompt: "使用 $deliver-task 完成这个由 caller 定义范围的软件开发交付。"
policy:
  allow_implicit_invocation: false
